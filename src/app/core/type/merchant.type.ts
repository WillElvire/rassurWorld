export type merchantWidget = {
  text: string;
  url: string;
  icon: string;
  color?: string;
};


export type merchantStatus = "Active" | "Desactive";

export type userType = "merchant" | "customer" | "agent";
